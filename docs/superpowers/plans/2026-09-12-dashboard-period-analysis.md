# Dashboard period analysis and current comparative Aging

> For agentic workers: use superpowers:subagent-driven-development for the independent tasks and review. Preserve the owner's existing authorization to develop, push, merge and deploy this new app.

**Goal:** Reports contains only External billing activity; Dashboard supports date ranges, closing-date open-invoice statistics, current comparative Aging and invoice drilldown.
**Architecture:** React/Vite frontend and one Cloudflare Worker; protected Supabase RPCs with additive private history. Reuse financial and verified-send readers without retaining their old report pages as destinations.
**Tech Stack:** React 19, TypeScript, Vite, Cloudflare Workers, Supabase PostgreSQL.
**Spec:** docs/PRODUCT_SPEC.md section13 + latest owner messages on 12 September2026 + decisions recorded below.

## Global constraints / rulings
- User chose this workspace. Work in place on codex/dashboard-period-analysis from952ed7b. No worktree or global config changes needed. Root owns Git/deployment/hosted mutations; implementers do not commit or write hosted state.
- Only NTHV9/ar-workspace (Public), Worker ar-workspace in account c82e3ca0932179eba19918aee28a9845 and Supabase jmyvpurzmoiecpydjrci. Preserve bindings with --keep-vars. No paid additions, resets, table drops, old AR services, mail sends or customer-file cleanup.
- Aging is CURRENT ONLY, unaffected by the period date range. Account Type and matched Account appear on the same row with TSK/KAT/Total. Reuse unambiguous Account No grouping and retain Hotel+Account ledger identity. Six source aging buckets, amounts and clearly scoped percentages; all types/accounts accessible, no Top N truncation. Invoice drilldown preserves filters and parent-child accounting safeguards.
- Period can be one day or inclusive from/to (Thailand calendar). All invoices = positive open root/standalone invoices AS OF the requested end date. No current-as-historical fallback. Missing old captures are unavailable, not zero. Show actual source/capture timestamps, not a claim of true midnight accounting balances.
- UI wording must explicitly mention Due date: use 'Past Due date' in the English UI (Thai owner wording: เลย Due date). OPERA invoice age >60 is separate.
- Closing-date counts/amounts: total open, billed Required, unbilled Required, Not Required, unknown setup, past Due date, OPERA age>60, age>60 and unbilled, latest Friendly/Follow-Up1/2/3/Final (dynamic active/historical policy labels). One latest stage per open invoice, no child duplication.
- Period activity: new source-date invoices including already-paid records; first billing/rebilling, actual follow-up sends; payments and linked invoice count using source payment dates. Payment-to-invoice allocation is the currently verified mapping for the dated payment cohort, not an invented application event date. Distinct invoice count and send occurrences must not be confused. Count partial payment as a paid-in-period invoice when mapping verifies it; keep cash/posting amount and mapped amount separate. Missing mapping/coverage remains explicit.
- Reports nav opens External billing activity only. Remove Current receivables/Verified sent activity/OPERA financial history/AR history & timing as standalone UI pages. Existing data services may continue feeding Dashboard. Old URL entry points should route safely to equivalent Dashboard detail or External billing; no dead navigation. Keep Account history functionality.
- Preserve the approved Luminous palette/typography, PDF/editor and unrelated pages. New supporting Dashboard layout is table-based and drillable, not dozens of tiny metric cards. Desktop1440x900 and laptop1280x800 plus usable narrow layout. No reference PNG edits.
- Store only essential private historical fields, bounded by existing database budgets and an explicit history growth cap if needed. Budget exhaustion must expose unavailable history without blocking OPERA current publication. No automatic historical row deletion. Do not backdate a new capture.
- Preflight hosted read:61 migrations through20260911104715; database61058195bytes,175accounts,1273invoices,2account-daycaptures on2026-09-11; dashboard_daily_invoices name absent. This is schema/count evidence, not financial validation.

## Task 1 — balance-at-date backend / shared contract
Owner: fresh backend implementer. Own worker/dashboard/*, one new Supabase migration, focused SQL/TS tests. Do not edit App.tsx, Dashboard frontend, root Worker router, docs or other migrations. Propose exact contract immediately; baseline shape below is binding unless coordinated.
- [ ] Inspect current publisher, invoice/workflow/exception schema, actor checks and quota protections. Use local CLI to generate migration filename; root applies it after review. No hosted writes.
- [ ] Add service-only authenticated read and bounded private daily invoice captures of actual verified publications, preserving snapshots and dates honestly. Capture actual current day only; historical gaps cannot be fabricated. Current-day read may use current saved state with timestamps; past day requires same-day capture. Consider workflow updates so captured billing/stage is not silently stale; expose source/capture timing.
- [ ] GET /api/dashboard/balances via exported dashboardBalancesApi(request,env,actor). Query asOf (required), hotel/type/account, metric or stage, page,limit. Strict validation. Export response types from worker/dashboard/model.ts for frontend. Root adds router entry.
- [ ] Response: {asOfDate,mode:'current'|'snapshot'|'unavailable',capturedAt,sourceAt,complete,missingHotels,reason?,metrics:[{key,count:number|null,amount:string|null}],stages:[{key,label,count:number|null,amount:string|null}],rows:DashboardBalanceRow[],total,unverified}. Row fields camelCase: hotel,accountId,accountNo,accountName,accountType,invoiceId,invoiceNo,folioNo,guest,transactionDate,open:string,original:string|null,age:number|null,billingRequired:boolean|null,firstBillingDate,dueDate,latestStage:string|null,latestStageLabel:string|null,latestSentAt,verified:boolean. Metric keys: open,billed,unbilled,not_required,setup,past_due,over60,over60_unbilled. No inaccurate sums on incomplete scope. Paging does not affect summary. Counts positive roots only; detailed unknown rows may remain explicit.
- [ ] Tests: historic-date isolation, unknown date, partial/failed refresh, actor/role denial, stage and billing boundaries, partial payment unrelated to zero, parent-child, full summary beyond page, cap behavior. Preserve all existing migrations/hooks. Supply reviewed migration and root validation instructions.

## Task 2 — current Aging comparison
Owner: fresh frontend implementer. Own src/dashboard/CurrentAging.tsx, aging-model.ts, aging.css, related unit/browser fixtures/tests only. Do not edit App.tsx, Dashboard.tsx, shared dashboard model/data/links, backend or docs.
- [ ] Props: {token:string;hotel:string;accounts:Account[];refresh?:RefreshState|null;onOpenInvoice:(hotel:string,accountId:string,invoiceId?:string)=>void}. Export default CurrentAging. Root passes complete catalog (not period-filtered), current global hotel and callback.
- [ ] Use existing sourceAging/aggregateAccounts authority. Current-only independent type/account drill context. TSK/KAT/Total in same comparison row for every source bucket. Preserve same account identity ambiguity checks against fullcatalog. Percentages explicitly describe denominator, handle credits/zero/unknown properly.
- [ ] Account Type -> matched Accounts -> Invoice list for selected hotel/bucket. Use the existing authenticated account endpoint (inspect App mapping); no arbitrary URL fetch. Exact membership, source-verification and root/child safeguards; distinguish missing hotel from verified zero. Show detail differences or unknown membership if source Account aging and invoice evidence cannot reconcile; never silently massage totals. Sorting, back/breadcrumb context, all rows/pagination, loading/error/empty/abort/race handling.
- [ ] Match incumbent tokens/components. Unit tests grouping/buckets/percentages/unknown/cross-hotel identity; browser drill/back/filter with synthetic fixtures. Report interface and any limits. No commit/deploy/hosted mutations.

## Task 3 — root period Dashboard, detail and Reports navigation
- [ ] Date model supports from/to and single-day presets with persisted URL and Back context. Header period scope is separate from current Aging scope.
- [ ] Compose closing-date balance matrix from Task1; period activity tables from existing source readers; detailed invoice/send/payment lists within Dashboard preserving range/scope. Include count and appropriate monetary basis per metric. Provide bounded source-range refresh using existing protected financial job API if coverage missing; do not open removed financial report page.
- [ ] Integrate CurrentAging as its own current view. Add dedicated date-independent tab/context. Update App navigation, remove lazy routes for retired report surfaces while keeping read endpoints and Account history. Reports resolves only External billing activity; repair old Dashboard/drill URLs.
- [ ] Tests range/date semantics, distinct counts, latest stage vs sends, history unavailable, payment dates/mappings, Reports-only-external, all drilldown/back paths, auth, desktop/laptop/narrow UI.

## Task 4 — review, live verification and delivery (root)
- [ ] Independent scoped backend/UI review plus integrated contract review. Resolve findings with targeted regressions.
- [ ] Inspect hosted schema before apply, apply additive migration, run SQL rollback fixtures/live read checks without exposing customer rows. Check advisors and database growth. No duplicate histories/fabricated date seeds.
- [ ] Build/typecheck/unit/SQL/browser tests. Inspect synthetic1440/1280/narrow evidence. Public audit all files/docs/images. Push correct remote branch, deploy exact SHA with --keep-vars, verify real health/auth and deployed browser assets/flows.
- [ ] CI-green expected-head merge. Record actual migration/version/SHA/tests/coverage limitations in PROJECT_STATUS, current decisions and acceptance notes. Stop after delivering this scope for owner review.
