# Shared lighting and depth across the application

Owner supplied a Billing Due card with gentle tonal lighting and shadow and explicitly requests the effect on every page. Refine the existing visual system; preserve layout and semantic hotel/status colors and all business behavior. Existing light mint Aging direction stays in force.

Owner subsequently requested Typography too. Add a dedicated shared UI type scale/selector map: retain Plus Jakarta Sans, improve small labels and hierarchy across routes, preserve exact all-range Aging fit and exclude authored PDF/email content.

## Implementation
- Shared CSS tokens for canvas light, subtle raised controls, content surfaces, summary cards and floating dialogs. A coherent upper-left light and soft downward shadows give separate elevation levels.
- Apply across Dashboard/Aging, Portfolio, Collections, Account and settings, Reports, Remittances, Templates, Storage, authentication/recovery, policy/operations, Email Composer and PDF workspace chrome.
- Exact mapped selectors and deliberate cascade specificity accommodate lazy page styles. Avoid blanket shadows on table rows, text links, icons, or nested financial cells; preserve selection marks, focus outlines, disabled/hover states and sticky geometry.
- Preserve existing gradients and opaque primary backgrounds. No white-label/transparent-button regression. Color panels retain their established semantic hues.
- PDF artwork, canvases, source text/layers, export bytes, charts and financial values are not restyled. No filter/backdrop-blur gimmicks, no fake trends and no layout/position transforms.
- Test actual app routes using synthetic fixtures. Capture representative desktop/laptop/narrow surfaces; verify computed elevation, control contrast and normal/hover/focus states plus existing behavior. Independent review before deploy.
- No backend/migration/provider/retention/resource changes. Only authorized NTHV9/ar-workspace (Public) and Cloudflare ar-workspace. Keep --keep-vars, private evidence and immutable reference PNGs.

## Work
- [x] Read-only route/selector and background audit (independent agent).
- [x] Root shared lighting stylesheet, independent typography stylesheet, targeted surface hooks if required, design documentation.
- [x] Independent cross-page synthetic visual/interaction tests.
- [x] Integrated review, build/typecheck/relevant tests and bounded visual confirmation.
- [ ] Audit public files, push/CI/deploy/live verify/merge and PROJECT_STATUS.
