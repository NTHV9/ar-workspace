import {afterEach,expect,it,vi} from 'vitest';
import {observationFilters,observationsApi} from '../worker/reports/observations';
afterEach(()=>vi.unstubAllGlobals());
it('keeps source timing, date and account scope explicit',()=>{expect(observationFilters(new URL('https://app.test/api/observations/timing?hotel=KAT&account=A&over60=true&page=2'))).toMatchObject({p_view:'timing',p_hotel:'KAT',p_account:'A',p_over60:true,p_offset:100});for(const query of ['account=A','hotel=OTHER','from=2026-02-30','over60=maybe','page=-1','hotel=KAT&hotel=TSK','from=2026-09-10&to=2026-09-01'])expect(()=>observationFilters(new URL('https://app.test/api/observations/daily_ar?'+query))).toThrow('observations_invalid');});
it('never manufactures an empty or zero report when the data service fails',async()=>{vi.stubGlobal('fetch',async()=>new Response('private details',{status:500}));const response=await observationsApi(new Request('https://app.test/api/observations/daily_ar'),{SUPABASE_URL:'https://synthetic.supabase.co',SUPABASE_SECRET_KEY:'synthetic'},'00000000-0000-4000-8000-000000000001');expect(response.status).toBe(503);expect(await response.json()).toEqual({error:'observations_unavailable'});});
